# OpenDAL Elixir Example

This example demonstrates how to use the OpenDAL Elixir bindings.

## Usage

1.  Navigate to the example directory: `cd examples/elixir`
2.  Install dependencies: `mix deps.get`
3.  Run the example: `mix run -e 'OpendalExample.run()'`
